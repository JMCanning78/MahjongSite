{{ #players }}
	<div class="player" data-name="{{ name }}">
		<button class="player-status player-status-{{ priority }}" 
			data-priority="{{ priority }}">
		    {{ ^seated }}<b>+</b>{{ /seated }}
		</button>
		<a href="playerstats/{{ playerid }}/latest"
		    <span class="player-name">{{ name }}</span>
		</a>
		<a class="deletebutton noselect clickable">✖</a>
	</div>
{{ /players}}
